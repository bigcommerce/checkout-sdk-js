export default interface InternalCurrency {
    code: string;
    decimal_places: number;
    decimal_separator: string;
    exchange_rate: string;
    symbol_location: string;
    symbol: string;
    thousands_separator: string;
}
