import { offsiteIntegration } from './offsite-integration';

describe('offsiteIntegration', () => {
    it('should work', () => {
        expect(offsiteIntegration()).toEqual('offsite-integration');
    });
});
