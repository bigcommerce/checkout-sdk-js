# headless-wallet-button-integration

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test headless-wallet-button-integration` to execute the unit tests via [Jest](https://jestjs.io).

## Running lint

Run `nx lint headless-wallet-button-integration` to execute the lint via [ESLint](https://eslint.org/).
